import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/**/!(*.test).(ts|js)"],
  format: ["esm", "cjs"],
  target: "node16",
  splitting: false,
  clean: true,
  dts: { resolve: true },
});
